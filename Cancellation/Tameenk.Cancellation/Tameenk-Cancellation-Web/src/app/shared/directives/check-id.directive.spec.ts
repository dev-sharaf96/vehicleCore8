import { CheckIdDirective } from './check-id.directive';

describe('CheckIdDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckIdDirective();
    expect(directive).toBeTruthy();
  });
});
